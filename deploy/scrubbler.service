[Unit]
Description=scrubbler — Last.fm metadata cleaner
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/opt/scrubbler
EnvironmentFile=/opt/scrubbler/.env
ExecStart=/usr/bin/node dist/index.js
Restart=always
RestartSec=15
# the service drains its in-flight edit on SIGTERM; give it room
TimeoutStopSec=90
# migrations run at app startup, so a plain restart is a full deploy step
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
